name: Build C Program

on:
  push:


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install GCC
      run: sudo apt update && sudo upt install -y gcc

    - name: Compile
      run: gcc main.c -o app

    - name: Run Program
      run: ./app